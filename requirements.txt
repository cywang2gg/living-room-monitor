# ─── 核心依賴 ───
opencv-python>=4.9.0.80
ultralytics>=8.2.0
numpy>=1.26.0
pyyaml>=6.0

# ─── 臉部辨識 ───
face-recognition>=1.3.0
# Windows 安裝需先: pip install dlib (需 CMake + VS Build Tools)

# ─── 語音提醒 ───
pyttsx3>=2.90

# ─── 桌面通知 (Windows) ───
win10toast>=0.9; sys_platform == "win32"

# ─── 開發測試 ───
pytest>=8.0
pytest-mock>=3.12
